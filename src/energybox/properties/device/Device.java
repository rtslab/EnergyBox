package energybox.properties.device;
// Only used to define a variable that later gets instanciated with one of the
// child classes in the properties.networks package
public abstract class Device{}
